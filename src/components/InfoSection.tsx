"use client";

import React from 'react';
import { motion } from 'framer-motion';

interface InfoSectionProps {
  show: boolean;
}

const InfoSection: React.FC<InfoSectionProps> = ({ show }) => {
  // Don't render anything - we now have badges above the prompt
  return null;
};

export default InfoSection; 